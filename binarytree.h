// Class for a binary search tree
#ifndef BINARYTREE_H
#define BINARYTREE_H

#include <stdio.h>

	/* Represents a tree Node */
	typedef struct Node {
		int data;
		struct Node *left;
		struct Node *right;
	} Node;	

#endif
